.env
node_modules/

# OS / editor / tool junk
.DS_Store
.vscode/
.cursorindexingignore

# Local agent / tooling caches (not for the team repo)
.playwright-mcp/
.specstory/
.vite/

# Local-only backup of the standalone reroom-frontend repo (superseded by reroom-frontend/)
reroom-frontend.local-backup/
# root-level design exports / QA artifacts (keep local, not in team repo)
/*.pdf
/*.png
/room[0-9].jpg
reroom-frontend.local-backup/
.specstory/

# Case study drafts — ALWAYS kept local (repo is public). The published case study
# lives on the portfolio site, not here. Matches CASE_STUDY* in any folder.
CASE_STUDY*

# Personal / local-only files (kept on this machine, not in the public repo)
/roombefore.jpg

# Local cleanup staging — review then delete by hand (never committed)
/_to-delete/
